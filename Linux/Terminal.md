# Terminal

Files and folders names which contain spaces are written using apostrophes or backslash (escape characters)

```bash
ls 'file name'
```

```bash
ls file\ name
```

To exit a terminal, use `exit` or `Ctrl+D`